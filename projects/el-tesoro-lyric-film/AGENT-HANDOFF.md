# El Tesoro preview handoff

**State:** complete playable preview, provisional text timing; cross-language listening review and production authorization remain open. The exact recording and presentation are frozen for review under `el-tesoro-preview-v1` in [the production gate](scripts/render-gate.ts). The [preview-first policy](../../docs/preview-before-render.md) controls later work.

1. Restore the local `public/source.mp4` matching [the manifest](source/media-manifest.json). It is excluded from Git. Run `npm ci`, `npm run build`, `npm run check`, then `npm run preview`; open <http://127.0.0.1:4327/>. Never substitute a recording merely because title and duration look similar.
2. Review the whole 4:31.534 soundtrack at normal speed in 16:9 and 9:16. Review all 24 cues and every Spanish/English focus event, then replay uncertain words and held endings at 0.75×/0.5×. The [review sheet](evidence/cross-language-sync-review.md) distinguishes model suggestions from actual listening. Focus first on sustained opening and middle `Ah`, L02 `qué`, L09/L20 `un poco acá`, L16→L17, repeated `el final`, and the last `épica`. Confirm the long coda has no new intelligible lyric.
3. If wording or timing changes, edit [the translation mapping](source/translation-mapping.json) or [provisional evidence](analysis/provisional-word-evidence.json), run `node scripts/build-timeline.ts`, `npm run build`, `npm run check`, and inspect both browser layouts again. Preserve independent timings for repeats. A changed preview reopens affected review.
4. Record genuine reviewer/method evidence and input hashes in `evidence/cross-language-sync-review.json` only after all checks are complete. Obtain explicit authorization for this exact revised preview in `evidence/render-authorization.json` before building a production renderer. `npm run render:production` is deliberately closed now, including for missing/stale files. Future capture must call [assertRenderGate](scripts/render-gate.ts) before its first frame and match the browser's source clock, typography, mapping and visual transform.
5. The source art is almost static. Browser playback was verified with nonblack picture and advancing decoded frames in both layouts; [the browser observation record](evidence/browser-preview-review.md) gives sampled checkpoints. Do not replace the complete preview with a lyrics-only diagnostic. The exact `public/source-poster.jpg` is a source frame, not proof of a completed film.

The brief behind this treatment is to integrate motion with the sword illustration and the measured arrangement: smoke moves gently in the existing clouds, a blade reflection follows transients, and text stays still. Audio features affect decoration only. The 10th/98th flux calibration corrected a former constant-glint state; `npm run check` now guards against recurrence. The final instrumental should feel intentionally open rather than gain fabricated subtitles.
