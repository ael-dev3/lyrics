import {revision} from '../src/identity.ts';
import {spawnSync} from 'node:child_process';import {mkdirSync,readFileSync,writeFileSync} from 'node:fs';import {createHash} from 'node:crypto';import {assertProductionGate} from './production-gate.ts';
assertProductionGate();const format=process.argv[2];if(format!=='landscape'&&format!=='portrait')throw Error('Expected format');const width=format==='landscape'?1920:1080,height=format==='landscape'?1080:1920,file=`output/Vedmy-${format}-${width}x${height}-60fps.mp4`,times=[0,8,28.3,45.65,65.7,78.9,94.55,117.83,122.4,134.6,159.3,175.6,182.35,195,201.133333],frames=times.map(t=>Math.round(t*60)),dir=`evidence/stills/production-${format}`;mkdirSync(dir,{recursive:true});const r=spawnSync('ffmpeg',['-v','error','-y','-threads','2','-i',file,'-vf',`select='${frames.map(f=>`eq(n,${f})`).join('+')}'`,'-fps_mode','vfr',`${dir}/frame-%02d.png`],{encoding:'utf8'});if(r.status!==0)throw Error(r.stderr);const inventory=frames.map((frame,i)=>{const path=`${dir}/frame-${String(i+1).padStart(2,'0')}.png`,b=readFileSync(path);return {frame,timeSeconds:frame/60,path,bytes:b.length,sha256:createHash('sha256').update(b).digest('hex')};});writeFileSync(`evidence/${format}-decoded-stills.json`,JSON.stringify({revision,source:file,frames:inventory,scope:'Frames decoded from the final delivery. Selected visual checkpoints supplement all-frame clock/focus checks and owner-attested acoustic review.'},null,2)+'\n');console.log({format,frames:inventory.length,directory:dir});
